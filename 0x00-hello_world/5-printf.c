#include <stdio.h>

/**
 * main - Prints line of texts.
 *
 * Description: Prints texts using `printf`.
 * Return: 0)
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}

